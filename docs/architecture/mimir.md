---
system: mimir
axes: [stub]
anchors:
  required:
    - package.json
decisions: []
reviewed: 2026-08-16
---

# mimir

Headless intelligence layer over FalkorDB. Episodic recall — what was discussed and when. Explicitly NOT a source of current architecture (ADR 001).

## STUB — anchors are real, the prose is not written yet

Scaffolded 2026-08-16 when the anchored-documentation discipline rolled out
(ADR 001, `gobot/docs/decisions/`). The `anchors:` above were verified to
exist, so this note is already load-bearing: delete or rename any of them and
`bun run arch:verify` fails. What is missing is the description.

Written deliberately as a stub rather than left absent. An absent note is
invisible; a stub appears in every session's system map saying plainly that
nobody has written this yet — which is true, and useful, and gets it fixed.

Fill in when someone next works here: what it does, its axes, what is
measured, what is open.
